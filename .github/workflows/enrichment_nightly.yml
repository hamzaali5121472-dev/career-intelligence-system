# DISABLED — replaced by daily_pipeline.yml
name: Enrichment Nightly (Disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use daily_pipeline.yml instead."
